.row

  .three.columns
    h3 Installed
    ul.square
      li(ng-repeat='(k,v) in installed'
         ng-click='selectInstalled(k)')
        {{k}}
    h3 Briqlets
    ul.square
      li(ng-repeat='id in briqlets'
         ng-click='selectBriqlet(id)')
        {{id.info.name}}
             
  .nine.columns(ng-show='selBriq')
    .panel
      h4 {{selBriq.info.name}}
      h4.subheader {{selBriq.info.description}}
      .row(ng-repeat='input in selBriq.info.inputs')
        .three.columns
          label.right.inline {{input.name}}:
        .nine.columns(ng-switch on='input.type')
          input.four(ng-switch-when='line'
                     type='text'
                     ng-model='input.value'
                     ng-disabled='selInst'
                     placeholder='{{input.default}}')
          textarea.eight(ng-switch-when='text'
                         ng-model='input.value'
                         ng-disabled='selInst'
                         rows=10)
      .row
        .nine.columns
          input.button(type='submit'
                       value='Install'
                       ng-click='installBriqlet()'
                       ng-disabled='selInst')
        .three.columns
          input.button.right(type='submit'
                             value='Remove'
                             ng-click='removeInstalled()'
                             ng-show='selInst')

.row
  .twelve.columns
    hr
    p.left Server platform: 
      i {{process.platform}}
      |  - it is now 
      b {{tick|date:'HH:mm:ss'}}
    p.right
      a(href='{{pkg.homepage}}') {{local.exactName}}
      |  {{pkg.version}}
